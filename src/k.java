public class k {
/*
    private void rotatingBalls() {
        e.setYOrd(250);
        if (attackComponents.size() == 1) {
            for (int i = 0; i < 30; i++) {
                attackComponents.add(new EnemyAttack());
            }

        } else {
            for (int i = 1; i < attackComponents.size()/2; i++) {
                EnemyAttack ea = attackComponents.get(i);
                ea.initBall(e.getXOrd() - 150 - e.getLength()/2  + 25*i, 0, 20, 5, 0);
                ea.setY(getCircularY(ea, false, e.getXOrd(), e.getYOrd()));
                if (ea.getY() == 0) System.out.println(i);
            }

            for (int i = attackComponents.size()/2 + 1; i < attackComponents.size(); i++) {
                EnemyAttack ea = attackComponents.get(i);
                ea.initBall(e.getXOrd() - 150  - e.getLength()/2 + 25*(i - attackComponents.size()/2), 0, 20, 5, 0);
                ea.setY(getCircularY(ea, true, e.getXOrd(), e.getYOrd()));
                if (ea.getY() == 0) System.out.println(i);
            }
            e.setVelX(e.getVel()*e.getDirX());
        }
    }

    EnemyAttack ea = new EnemyAttack();
            ea.initBall(e.getXOrd() - 150  + 7, 0, 20, 5, 0);
            ea.setY(getCircularY(ea, false, e.getXOrd(), e.getYOrd()));
            attackComponents.add(ea);

    ea = new EnemyAttack();
            ea.initBall(e.getXOrd() - 150  + 343, 0, 20, 5, 0);
            ea.setY(getCircularY(ea, false, e.getXOrd(), e.getYOrd()));
            attackComponents.add(ea);

    ea = new EnemyAttack();
            ea.initBall(e.getXOrd() - 150  + 7, 0, 20, 5, 0);
            ea.setY(getCircularY(ea, true, e.getXOrd(), e.getYOrd()));
            attackComponents.add(ea);

    ea = new EnemyAttack();
            ea.initBall(e.getXOrd() - 150  + 343, 0, 20, 5, 0);
            ea.setY(getCircularY(ea, true, e.getXOrd(), e.getYOrd()));
            attackComponents.add(ea);*/
}
